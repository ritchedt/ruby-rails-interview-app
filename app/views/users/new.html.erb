<div class="text-center">
  <h1>Create New Users</h1>
</div>
<div>
  <%= form_for(@user) do |f| %>
    <%= render 'create_fields', f: f %>
    <%= f.submit "Create a new User", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
<br>
<%= link_to 'Back', users_path %>
